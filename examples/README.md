# GitHub Issue MCP Examples

This directory contains examples showing how to use `github-issue-mcp` as a reusable MCP server.

## Prerequisites

Node.js 20+ is recommended.

Install the package:

```bash
npm install github-issue-mcp